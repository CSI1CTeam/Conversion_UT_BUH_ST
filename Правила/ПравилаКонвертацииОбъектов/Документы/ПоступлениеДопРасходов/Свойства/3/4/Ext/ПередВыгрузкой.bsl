Если ТипЗнч(ОбъектКоллекции.ДокументПартии) = Тип("ДокументСсылка.КомплектацияНоменклатуры") Тогда
	ИмяПКО = "КомплектацияНоменклатуры";
ИначеЕсли ТипЗнч(ОбъектКоллекции.ДокументПартии) = Тип("ДокументСсылка.ПоступлениеТоваровУслуг") Тогда
	ИмяПКО = "ПоступлениеТоваровУслуг";
Иначе
	Отказ = Истина;
КонецЕсли;